# MDM (Model Oversight Engine) — örnek ortam değişkenleri (API key / token yok)
# Kopyala: cp .env.example .env ve gerekirse değerleri düzenle.

# L2 Onayla/Red log dosyası (varsayılan: review_log.jsonl)
# MDM_REVIEW_LOG=review_log.jsonl

# Config profil (örn. wiki_calibrated, scenario_test, production_safe)
# MDM_CONFIG_PROFILE=wiki_calibrated

# Invariant assert (1/true/yes = karar sonrası invariant kontrolü)
# MDM_ASSERT_INVARIANTS=

# Git commit (audit paketine yazılır, opsiyonel)
# MDM_GIT_COMMIT=

# Geliştirme modu (1 = bazı ek importlar)
# MDM_ENGINE_DEV=
